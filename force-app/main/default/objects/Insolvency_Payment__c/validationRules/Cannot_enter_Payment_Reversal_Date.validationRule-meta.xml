<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Cannot_enter_Payment_Reversal_Date</fullName>
    <active>true</active>
    <description>Payment Reversal Date cannot be entered for non reversed payment</description>
    <errorConditionFormula>AND( ISBLANK( TEXT(Payment_Reversed__c )) ,  NOT( ISBLANK( Payment_Reversal_Date__c ) ) )</errorConditionFormula>
    <errorMessage>Payment Reversal Date cannot be entered for non-reversed payment</errorMessage>
</ValidationRule>
