<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Assign_only_to_one_parent</fullName>
    <active>false</active>
    <description>Settlement record cannot be linked to both Judgement and Product records</description>
    <errorConditionFormula>(
IF(ISBLANK( Judgment__c ), 0,1)+
IF(ISBLANK( Product__c ), 0,1)
)  &lt;&gt;  1</errorConditionFormula>
    <errorMessage>Settlement cannot be linked to both Judgement and Product records</errorMessage>
</ValidationRule>
