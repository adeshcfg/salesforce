/**************************************************************************************************
 *
 *  Class:   
 *  @description : This is a Test class for DeleteTasks Batch
 *  @author : A5E Consulting
 *  @date : 06/15/2020
 
 *************************************************************************************************/
 @isTest
public class TestDeleteTasks {
    public static testMethod void testDeleteTasks(){
        Judgment__c judgmentInstance = new Judgment__C();
        judgmentInstance.Legal_Agency__c = '29';
        insert judgmentInstance;
        list<Task> listTask = new list<Task>();
        for(integer i=0; i < 10; i++){
            Task taskInstance = new Task();
            taskInstance.status = 'Generated by Docomotion';
            listTask.add(taskInstance);
        }
        insert listTask;
        
        Test.startTest();
        DeleteTasks obj = new DeleteTasks();
        database.executeBatch(obj);
        Test.stopTest();
    }
}